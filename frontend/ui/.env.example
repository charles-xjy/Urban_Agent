# 浏览器侧 API 地址。
# 推荐 /api（走 Next.js 同源代理，本机和局域网访问都可用，无需写死服务器 IP）。
# 也可直连后端，如 http://localhost:2024（仅本机访问时有效）。
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_ASSISTANT_ID=agent

# 服务端代理目标：Next.js 把 /api/* 转发到该地址（LangGraph Server）。
# 前端与后端同机部署时保持 localhost 即可。
LANGGRAPH_API_URL=http://localhost:2024

# 项目亮点

本项目的核心亮点，不是单纯“能做城市变化分析”，而是把整条研究链路做成了一个可控、可审计、可并行的流程。  
从用户输入一个 `query` 开始，系统会依次完成自然语言解析、问题澄清、研究计划生成、用户审批、固定维度并行研究、多源证据验证和最终报告汇总。下面按真实执行顺序说明每个部分的实现与作用。

## 1. 用户输入 Query：自然语言入口

用户在 `main.py` 中直接输入自然语言问题，例如：

`分析雄安新区 2018-2024 年的城市变化`

这一层的作用不是立即开始研究，而是先把模糊需求变成结构化任务。  
当前实现中，`main.py` 会先通过一个轻量的 LLM 解析步骤，把用户输入拆成：

- `location`：地点
- `start_year`：起始年份
- `end_year`：结束年份

### 实现作用

- 降低后续流程的歧义
- 让所有研究维度共享统一的时空边界
- 为后续计划生成、证据检索和报告汇总提供统一上下文

### 对应实现

- [main.py](D:/mycode/Urban%20Agent/main.py)
- 解析逻辑：`_parse_query()`

---

## 2. 问题澄清：先确认输入是否足够

在进入正式研究前，主图会先执行 `clarify` 节点。  
它的任务不是研究事实，而是判断当前 query 是否已经包含足够信息，至少要有：

- 地点
- 时间范围

如果信息不完整，系统会中断并追问一次，要求用户补充关键信息。

### 为什么要先澄清

如果一开始输入就不完整，后续生成的研究计划会基于错误边界，导致：

- 研究维度不准确
- 工具调用浪费
- 最终结论不可复现

### 实现作用

- 保证输入边界清晰
- 避免无意义的后续计算
- 让研究从一开始就建立在可执行条件上

### 对应实现

- [graph/main_graph.py](D:/mycode/Urban%20Agent/graph/main_graph.py)
- 节点函数：`clarify_node()`
- 中断机制：`interrupt()`

---

## 3. Planner：把“城市变化”拆成可执行维度

当输入条件齐备后，系统进入 `planner` 节点。  
这个节点不会直接做分析，而是生成 2-4 个固定研究维度，把一个大问题拆成多个可执行子任务。

例如，针对“城市变化”这类问题，Planner 可能拆成：

- 城市空间扩张
- 交通网络演进
- 生态格局变化
- 公共服务设施布局

### 这一层的作用

这是整个项目最重要的流程控制点之一。  
它把“开放式研究”变成“预定义维度研究”，带来三个直接好处：

- 研究路径可控：先定义研究什么，再执行
- 结果更可比：不同维度之间是并列关系，不会混成一团
- 过程更透明：每个结论都能回溯到具体任务

### 实现作用

- 将宽泛 query 变成结构化 `ResearchTask` 列表
- 让后续并行研究有明确分工
- 防止模型在执行中临时发散出新方向

### 对应实现

- [graph/main_graph.py](D:/mycode/Urban%20Agent/graph/main_graph.py)
- 节点函数：`planner_node()`
- 任务结构：`ResearchTask`

---

## 4. 用户审批：把研究边界前置给用户

Planner 生成研究计划后，不会立刻执行，而是进入 `human_approval` 节点。  
这个节点会把计划完整展示给用户，由用户确认或修改。

用户可以：

- 直接确认执行
- 替换某些研究维度
- 合并研究维度
- 删除不关心的维度
- 用 JSON 或自然语言重写计划

### 这一层的作用

这一步把“研究维度选择权”前置给用户，避免模型替用户决定研究重点。  
对城市变化分析来说，这一点尤其重要，因为用户真正关心的往往不是“所有变化”，而是某类变化，例如：

- 产业扩张
- 公共服务设施
- 交通可达性
- 生态空间变化

### 实现作用

- 保证研究方向可审查
- 允许用户主动调整研究重点
- 把“计划生成”与“计划执行”分离开

### 对应实现

- [graph/main_graph.py](D:/mycode/Urban%20Agent/graph/main_graph.py)
- 节点函数：`human_approval_node()`
- 中断与恢复：`interrupt()` + `Command(resume=...)`

---

## 5. 固定维度并行验证：多个 Researcher 同时工作

一旦用户确认计划，系统会通过 `Send()` 把每个 `ResearchTask` 派发给独立的 `researcher` 节点。  
这就是“固定维度并行验证”的实现。

每个 Researcher 只负责一个维度，不跨维度抢任务，也不临时生成新的研究方向。

### 这一层的作用

- 并行提高效率
- 降低任务耦合
- 让每个维度的证据链清晰独立
- 方便后续汇总时逐项审查

### 为什么不用动态任务委派

传统的动态委派方式会让研究路径在执行中不断变化，容易出现：

- 新任务不断冒出来
- 研究范围失控
- 证据链断裂
- 结果难以复现

本项目反过来做：先固定维度，再并行执行。这样虽然少了些“开放性”，但换来的是强控制和高可追溯性。

### 对应实现

- [graph/main_graph.py](D:/mycode/Urban%20Agent/graph/main_graph.py)
- 函数：`dispatch_researchers()`
- 并行机制：`Send("researcher", {...})`

---

## 6. Researcher 节点：每个维度内部的证据驱动 ReAct 循环

每个 Researcher 实例对应 [graph/researcher_graph.py](D:/mycode/Urban%20Agent/graph/researcher_graph.py) 里的 `researcher_node()` 函数。  
它不是一次性写结论，而是在一个循环里不断调用工具、收集证据、验证证据，再形成结论。

> **注意**：这里是一个普通节点，内部用 `create_react_agent()` 构建了预置 ReAct agent，  
> 不是 LangGraph 独立编译的 StateGraph 子图，两者在 checkpoint 和状态隔离上有本质区别。

### Researcher 的任务结构

系统会把以下信息一起传给 Researcher：

- 当前任务 `task`
- 地点 `location`
- 起始年份 `start_year`
- 结束年份 `end_year`
- 当前累计工具调用次数 `runtime`

### 30 次工具调用硬限制

`researcher_node()` 读取 `state.get(“runtime”, 0)` 作为 `runtime_offset`，并在传给 agent 的 query 里写入：

```
当前已累计工具调用次数：X（超过 30 次未定论须输出冲突声明）
```

agent 在 prompt 里看到这个数字，超过 30 次仍无定论时必须停止并输出冲突声明，不得继续调用工具。  
这是防止 agent 陷入无限搜索循环的硬约束机制，通过 `config={“recursion_limit”: 80}` 兜底。

### findings 并发安全追加

主图 `AgentState` 中 `findings` 字段声明为：

```python
findings: Annotated[list[str], operator.add]
```

LangGraph 用 `operator.add` 作为 reducer，多个并发 researcher 节点各自返回 `{“findings”: [...]}` 时，  
框架自动追加合并，不会产生写冲突。每个 researcher 返回格式为：

```python
{“findings”: [f”=== {task['topic']} ===\n{findings}”], “runtime”: tool_call_count}
```

### 这一层的作用

Researcher 的职责不是”自由发挥”，而是：

- 围绕单一维度做证据收集
- 根据证据状态决定下一步工具
- 在证据充分时结束，不继续发散

### 对应实现

- [graph/researcher_graph.py](D:/mycode/Urban%20Agent/graph/researcher_graph.py)
- 函数：`researcher_node()`
- Agent 构建：`create_react_agent()`

---

## 7. 三路证据链：搜索证据、遥感证据、POI 证据

这是项目的第二个核心亮点。  
Researcher 不是只查网页，而是围绕三类证据形成互证链路。

### 7.1 搜索证据

由 `web_search` 和 `web_fetch` 提供，主要用于：

- 获取政策文本
- 获取新闻报道
- 获取统计公报
- 获取规划和官方说明

它解决的是”发生了什么、为什么发生、官方怎么说”的问题。

#### 域名权威度评分

`web_search` 不是简单返回搜索结果，而是对每条结果做域名权威度打分，结果按分数降序排列后才返回给 Researcher：

| 域名类型 | 分数 | 标签 |
|---|---|---|
| stats.gov.cn / data.stats.gov.cn | 1.0 | 国家统计局 |
| gov.cn | 0.95 | 政府官网 |
| xinhuanet.com / people.com.cn 等 | 0.85 | 官方媒体 |
| .edu.cn / .ac.cn / cnki.net 等 | 0.80 | 学术机构 |
| sina / sohu / 163 / qq / toutiao 等 | 0.45 | 门户/聚合 |
| 其他 | 0.40 | 普通网站 |

返回结构中每条结果带 `source_score` 和 `source_label` 字段，Researcher 可据此判断文字证据的可信程度。  
这是搜索层面的第一道质量过滤，在 agent 决策之前就完成了来源排序。

#### 对应实现

- [tools/web_search.py](D:/mycode/Urban%20Agent/tools/web_search.py)（含 `_score_domain()` 评分函数）
- [tools/web_fetch.py](D:/mycode/Urban%20Agent/tools/web_fetch.py)（通过 Jina Reader 抓取完整页面）

#### 作用

- 建立背景知识
- 作为事实判断的文字证据，并附带来源权威度分数
- 为后续遥感和 POI 证据提供解释框架

### 7.2 遥感证据

由 `analyze_satellite_image` 提供。  
它只负责比较两期卫星图像的可见变化，不做原因推断。

例如它可以描述：

- 建成区扩张
- 植被减少
- 水体变化
- 道路或裸地增多

但它不会直接说”这是因为开发””这是因为产业升级”。

#### 影像获取链路

工具内部执行三步：

1. **高德 Geocode API** — 将地名转换为经纬度坐标
2. **Google Earth Engine** — 并发下载起止两年的卫星影像（配置项：`GEE_PROJECT`、`IMAGE_BUFFER_METERS=1500`、`IMAGE_DIMENSION=512`、`IMAGE_CLOUD_PCT≤10%`）
3. **视觉模型推理** — 对两张图做对比分析

#### 双模型并行调用设计

这是整个工具最关键的实现细节：每次 `analyze_satellite_image` 调用会同时向两个不同 system prompt 发起推理，结果完全独立：

| 调用 | System Prompt 约束 | 接收方 | 用途 |
|---|---|---|---|
| `_vision_invoke(_VISUAL_SYSTEM, ...)` | 只描述像素颜色/纹理/形状，禁止解释原因 | Researcher | 防止视觉模型越权推断，结论由 Researcher 结合其他证据自行得出 |
| `_vision_invoke(_BASELINE_SYSTEM, ...)` | 完整推断分析，可下结论 | 不给 Researcher 看 | 存入 `data/baselines/` 作为 eval 对照基准 |

Researcher 拿到的是纯像素级描述，完整分析报告则以时间戳命名异步存档，用于后续评估实验对比主 agent 结论与视觉模型直接结论的差异。  
两个推理并行发起（`asyncio.gather`），不增加额外延迟。

视觉模型使用端口 8002 的小模型（`CLAIM_MODEL_URL`），与主 agent 使用的 8001 模型分开部署，见下文”两模型部署”说明。

#### 对应实现

- [tools/satellite.py](D:/mycode/Urban%20Agent/tools/satellite.py)
- 视觉观察函数：`_vision_invoke()`
- 基准存档函数：`_save_baseline()`

#### 作用

- 验证空间变化是否真实存在
- 防止视觉模型把”看见变化”误当成”知道原因”
- 避免季节、阴影、纹理造成的误判
- 同步生成 eval baseline，供后续实验分析

### 7.3 POI 证据

由 `query_poi_history` 提供，主要用于量化实体变化，例如：

- 建筑数量变化
- 医院数量变化
- 学校数量变化
- 商业、住宅、道路、工业设施变化

#### 三级 fallback 架构

POI 工具不依赖单一数据源，按以下顺序降级：

**路径 A — ohsome API（主路径）**

- 高德 Geocode → 经纬度 → 生成 bbox（`IMAGE_BUFFER_METERS=1500` 米半径）
- 向 `api.ohsome.org/v1/elements/count` 查询 OSM 历史快照，时间点固定为各年 7 月 1 日
- 返回起止两年计数、变化量、变化率，附置信度 **0.8**（OSM 众包，中国覆盖率有限）

**路径 B — 高德 POI 实时搜索（fallback）**

触发条件：ohsome 两端计数均为 0，或高德 Geocode 失败。  
调用高德文本搜索 API，按地名和 POI 类型码查询当前数量。  
缺点：只有当前快照，**无历史对比**，置信度 **1.0**（商业采集，实时准确）。  
工具会在返回值中明确说明"仅为当前快照"，提示 Researcher 补充 web_search。

**路径 C — 完全失败**

两条路径均无数据时，明确返回"建议改用 web_search 获取文字证据"，不输出任何数字，  
置信度标记为 **0.0**。

工具支持 10 种 OSM 类别（`building / road_primary / road_secondary / hospital / school / residential / commercial / park / water / industrial`），部分类别仅有 ohsome 路径（如 `building`、`road`），无高德映射。

#### 对应实现

- [tools/poi.py](D:/mycode/Urban%20Agent/tools/poi.py)
- ohsome 查询函数：`_ohsome_count()`
- 高德 fallback 函数：`_gaode_poi_count()`

#### 作用

- 提供量化支撑，并附带置信度分数
- 验证实体增长或减少趋势
- 补足文字和图像之外的结构性证据

---

## 8. 置信度机制：证据不是平均，而是分级融合

项目不是把三类证据简单平均，而是根据互证程度做分级判断。

> **实现说明**：置信度规则和冲突处理规则全部写在 `researcher_graph.py` 的 `_RESEARCHER_SYSTEM` prompt 里，  
> 由 LLM 自身执行。这是 **prompt engineering**，不是代码层面的分支逻辑。  
> 工具层面（`web_search` 的 `source_score`、`query_poi_history` 的置信度字段）返回的量化分数  
> 是给 LLM 参考的输入信号，最终融合判断由 LLM 完成。

### 规则大致如下

- 三路证据相互印证：高置信度，可明确写结论
- 两路证据支撑：中置信度，可正常陈述但保留边界
- 仅一路证据：低置信度，只能说”目前仅有 X 类证据支持”
- 无有效证据：不输出确定性结论，直接说明证据不足

### 这一层的作用

- 抑制幻觉
- 避免过度解释
- 降低单一模态误导
- 让最终结论有明确证据边界

### 冲突处理

如果证据之间出现冲突，系统不会强行统一，而是按顺序尝试消解：

1. 再次 `web_search`，关键词加官方统计机构名，寻找第三方裁定来源
2. 调用 `query_poi_history` 获取量化数据，作为独立仲裁证据
3. 再次调用 `analyze_satellite_image`，换时间节点或更小区域重新观测

经过以上三步或累计调用超过 30 次仍无法消解时，明确输出：

```
【冲突未解决】证据存在冲突，暂无法定论
- 支持”[结论A]”的证据：[来源及内容]
- 支持”[结论B]”的证据：[来源及内容]
```

而不是硬给结论。

---

## 9. Reporter：把所有 findings 汇总成最终报告

当所有 Researcher 完成后，系统会进入 `reporter` 节点。  
这个节点只做汇总，不再新增证据。

它会把各个维度的 `findings` 整理成一个完整的城市变化分析报告。

### 这一层的作用

- 统一输出格式
- 将多维研究结果整合为完整叙事
- 保证最终报告来源清晰、边界明确

### 对应实现

- [graph/main_graph.py](D:/mycode/Urban%20Agent/graph/main_graph.py)
- 节点函数：`reporter_node()`

---

## 10. 基础架构支撑：使上述流程得以运行的技术前提

### 10.1 双 LLM 部署（职责分离）

系统部署了两个独立的 vLLM 实例：

| 实例 | 地址 | 承担职责 |
|---|---|---|
| 主 agent 模型 | `10.129.107.145:8001` | clarify / planner / human_approval / researcher ReAct / reporter |
| 视觉小模型 | `10.129.107.145:8002` | 卫星图双模型推理（pixel-only 和 baseline） |

两个模型名称通过启动时自动 curl `/v1/models` 获取（`_fetch_model_name()`），也可在 `.env` 里手动指定 `AGENT_MODEL_NAME` / `CLAIM_MODEL_NAME` 覆盖。

视觉任务使用独立端口的小模型，避免占用主 agent 模型的推理资源，两者可并发调用互不阻塞。

### 10.2 MemorySaver checkpointer 支撑 interrupt/resume

主图编译时挂载了 `MemorySaver()`：

```python
g.compile(checkpointer=MemorySaver())
```

这是 `clarify` 和 `human_approval` 两个节点中 `interrupt()` 能正常暂停、用户输入后 `Command(resume=...)` 能正常恢复的技术前提。  
没有 checkpointer，图的中间状态无法持久化，整个人机交互机制就无法工作。

thread_id 格式为 `run_{location}_{start_year}`，同一地点时间段的会话共享同一个检查点。

### 10.3 batch_mode：跳过交互步骤的批量实验模式

`AgentState` 里有 `batch_mode: bool` 字段。置为 `True` 时：

- `clarify_node()` 直接返回，不做 LLM 判断
- `_after_planner()` 路由函数跳过 `human_approval`，直接调用 `dispatch_researchers()`

这个模式通过 `batch_run()` 函数暴露，用于对照实验和自动化评估，不需要人工干预即可批量运行。

---

## 11. 这条流程为什么是亮点

如果把整个项目压缩成一句话，就是：

> 从一个自然语言 query 出发，先生成可审批的研究计划，再用固定维度并行研究和三路证据互证，最终输出可追溯的城市变化报告。

它的亮点不在于“模型更大”，而在于“流程更稳”：

- 输入端先结构化，避免歧义
- 计划端先审批，避免发散
- 执行端固定维度并行，避免任务失控
- 证据端三路互证，避免幻觉
- 输出端只汇总已验证 findings，避免越权推断

这使得项目更像一个城市变化研究系统，而不是一个单纯的聊天式问答程序。

